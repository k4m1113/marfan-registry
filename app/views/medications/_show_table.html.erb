<%= render "layouts/show_table", discussion_topic: medications %>

<!-- <div class="card">
  <div class="card-header">
    Medications
  </div>
  <div id="medsCollapse" class="collapse show" role="tabpanel" aria-labelledby="medsHeading">
    <% if medications.length != 0 %>
      <table class="table table-striped table-responsive">
        <thead>
          <tr>
            <%= Medication.table_headings.each do |th| %>
              <th>
                <%= th %>
              </th>
            <% end %>
          </tr>
        </thead>
        <% medications.each do |m| %>
          <tr>
            <% row =  m.table_body %>
            <% row.each do |k, v| %>
              <td>
                <%= v %>
              </td>
            <% end %>
          </tr>
        <% end %>
      </table>
    <% else %>
      Patient does not have any meds on file yet. <%= link_to "Add Some", new_medication_path(@patient), class: "" %>
    <% end %>
  </div>
</div> -->
