<table>
  <tr>
    <th>Visit ID</th>
    <th>Timestamp</th>
    <th>Patient ID</th>
    <th>Actions</th>
  </tr>
  <% @visits.each do |visit| %>
  <tr>
    <td><%= visit.id %></td>
    <td><%= visit.created_at %></td>
    <td><%= link_to visit.patient_id, patient_path(visit) %></td>
    <td><%= link_to "Edit Visit", edit_visit_path(visit.id) %></td>
  </tr>
  <% end %>
</table>
