<div class="btn-group" role="group">
  <%= link_to edit_genetic_test_path(id: t.id), method: :get, class: "btn btn-light btn-sm" do %>
    <i class="fa fa-pencil"></i>
  <% end %>
  <%= link_to genetic_test_path(id: t.id), method: :delete, class: "btn btn-light btn-sm", data: { confirm: "Are you sure you want to delete genetic test record #{t.id} for #{t.topic.name} from record of #{t.patient.full_name}?"} do %>
    <i class="fa fa-trash"></i>
  <% end %>
</div>
